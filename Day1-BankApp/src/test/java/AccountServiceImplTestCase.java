import org.cap.dto.Account;
import org.cap.exception.InsufficientBalanceException;

public class AccountServiceImplTestCase {
	/*public void withdraw(int accountNo, double amount) throws InsufficientBalanceException {
		A	ccount account=accountDao.findAccountById(accountNo);
		
		if(account.getAmount()<amount)
			throw new InsufficientBalanceException();
		
		account.setAmount(account.getAmount()-amount);
		
		
		return account;
	}*/
}
